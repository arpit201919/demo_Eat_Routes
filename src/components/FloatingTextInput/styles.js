import React from "react";
import { TextField } from 'rn-material-ui-textfield'

const FloatingTextInput = ({ placeholder }) => {
    return (<TextField
        placeholder={placeholder}
    />
    )
}

export default FloatingTextInput;